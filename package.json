{
    "name": "bluetooth-controller",
    "description": "Demo app to control ESP32 devices through bluetooth. Uses esbuild as bundler with native Typescript and Web Components.",
    "license": "MIT",
    "author": "Ryan Weiss (rw3iss@gmail.com)",
    "version": "0.0.0",
    "engines": {
        "node": ">=20"
    },
    "scripts": {
        "dev": "node scripts/dev.js --config tsconfig.json",
        "build": "node scripts/build.js --config tsconfig.json",
        "analyze": "cem analyze --litelement",
        "start": "tsc && concurrently -k -r \"tsc --watch --preserveWatchOutput\" \"web-dev-server\"",
        "serve": "web-dev-server "
    },
    "dependencies": {
        "dotenv": "^16.4.7",
        "lit": "^3.1.4"
    },
    "devDependencies": {
        "@custom-elements-manifest/analyzer": "^0.10.3",
        "@web/dev-server": "^0.4.6",
        "concurrently": "^8.2.2",
        "esbuild": "^0.24.2",
        "esbuild-node-externals": "^1.16.0",
        "esbuild-sass-plugin": "^3.3.1",
        "fs-extra": "^11.2.0",
        "tslib": "^2.6.3",
        "typescript": "^5.5.3"
    },
    "customElements": "custom-elements.json"
}